# Build frontend and deploy with Laravel backend
[phases.setup]
nixPkgs = ["nodejs", "php83", "php83Packages.composer"]

[phases.install]
cmds = [
  "rm -rf node_modules",
  "npm install --no-optional",
  "npm install @rollup/rollup-linux-x64-gnu @swc/core-linux-x64-gnu",
  "cd backend && composer install --no-dev --optimize-autoloader"
]

[phases.build]
cmds = [
  "npm run build",
  "ls -la dist/",
  "cp -r dist/* backend/public/",
  "ls -la backend/public/"
]

[start]
cmd = "cd backend && php artisan serve --host=0.0.0.0 --port=8080"

